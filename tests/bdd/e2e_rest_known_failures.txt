# Known-failing e2e_rest BDD scenarios — entries that genuinely fail against the
# live server, in three categories (production gap / harness gap / upstream
# scenario defect). NOT a "production gaps only" list: see the composition below.
#
# These dispatch through the real server over HTTP. After Wave 3 triage (#1418)
# every mock-injection-only entry was either graduated (it now passes in-network)
# or moved to an env-level E2EUnsupportedSetup declaration (raised during harness
# realization in tests/harness/, surfaced as xfail by the conftest report hook —
# NOT listed here). An entry leaves this file on POSITIVE in-network evidence only:
# it xpasses in the bdd-in-network job (graduation), or its setup method provably
# has no live-server surface (route 3, E2EUnsupportedSetup declared at the method
# and pinned in tests/unit/test_architecture_e2e_rest_escape_hatches.py). Never on
# assertion alone. Each block below is annotated with the reason it stays.
#
# Loaded by tests/bdd/conftest.py to xfail(strict=False) exactly these nodeids.
# Locked by tests/unit/test_e2e_rest_ledger_state.py — keep that test in sync.
#
# CI-GRADED: the "BDD In-Network (e2e_rest)" job in .github/workflows/ci.yml
# runs the bdd suite inside the compose network with BDD_E2E_ENABLED=true, so a
# ledger graduation (or regression) fails CI. Historical removals below cite the
# manual in-network gates (innet_* report ids) that verified them before the CI
# job existed. To make it merge-blocking, add the job to the repo's required
# status checks (branch-protection setting, not workflow-controlled).
# Design + execution plan + per-mechanism breakdown:
#   docs/test-redesign/e2e-rest-ledger-retirement.md
#
# Wave-3 graduation (#1418, against the in-network wave2 report): removed 163
# now-passing entries, 4 stale (renamed by #1370), and 98 format-injection
# entries now owned by the env E2EUnsupportedSetup declaration. That wave left
# 47 entries; the count has moved since — the CURRENT composition is below.
#
# CURRENT SET: 14 entries, by category —
#   7  upstream SCENARIO defects (#1756): the dispatched request field exists
#      nowhere in pinned adcp v3.1.1 and the live server correctly answers
#      INVALID_REQUEST. sampling_method x6 + the ownership BOUNDARY row.
#      Retiring these means reconciling the generated scenario upstream, NOT
#      implementing a non-spec field.
#   6  production gaps: include_package_daily_breakdown not computed (#1711, x2),
#      two uc004 invalid inputs the live server still accepts (string "true"
#      daily-breakdown, geo metro without system), and the 2 inline-xfail rows
#      (seller attribution default, push-notification ack).
#   1  contested: the ownership PARTITION row is annotated below as a
#      live-server-accepts validation gap, but it dispatches the same non-spec
#      `ownership` field as the #1756 group. Classify it in #1756, not here.
#   0  harness gaps — none of the 14 is a harness/observability gap today.
# History of the count: 17 after the main merge (the owner-approved union added
# the 10 parallel-e2e rows) -> 24 at the build_rest_body reconciliation (#1600:
# graduated 2, unmasked 9) -> 14 here, graduating those 10 parallel-e2e rows on
# in-network evidence (#1585 A4, block below).
# The 4 uc004 date-range rows and the 2 uc004 account valid rows GRADUATED at the
# #1417 merge and must never be re-added.

# --- uc004 invalid-input validation — wire shape — GRADUATED (#1430 jdy1-M1) ---
# Fixed by mapping FastAPI 422 -> AdCPValidationError in parse_rest_error
# (tests/harness/_base.py); the 6 entries now xpass in-network (innet_120626_1848).

# --- uc004 invalid-input validation — accepted by live server (3) ---
# GRADUATED 2 of the original 5 (build_rest_body fix, #1600; in-network innet_280726_1823): the owner-mismatch BOUNDARY
# row and the out-of-enum sampling row. The live server was never given the chance to reject them —
# the harness build_rest_body DROPPED raw field kwargs, so `ownership` and `sampling_method` never
# reached the wire and a 200 came back. With the drop removed both are rejected and both XPASSed
# strictly. Their conftest strict-xfail tripwires are removed in the same change. NOTE the
# ownership PARTITION twin below did NOT graduate and stays: it is a different row and still
# xfails.
# GENUINE GAP: the live server accepts these inputs that the spec marks invalid
# (owner-mismatch principal, out-of-enum sampling, string "true" daily-breakdown,
# geo metro without system).
# Production does not validate them — a real spec-production gap, tracked for src/ fix.
# (campaign-unit interval!=1 GRADUATED at the main merge: upstream regenerated the
# scenario's expected cell `invalid` -> `error "VALIDATION_ERROR" with suggestion`,
# the old `-invalid]` nodeid no longer collects, and the renamed scenario passes
# in-network on main — absent from main's ledger.)
# date-range rows GRADUATED on both sides: the BOUNDARY rows on this branch's
# first in-network CI run 2026-07-09 (the #1270 strict tripwires FIRED — the
# live server validates start>=end, the merged #1417 validation embed); the
# partition twins (start_after_end / start_equals_end) on origin/pr-1417 itself
# (d4af23095, strict-xfail XPASS in-network) — that is the positive evidence the
# twins were waiting for, so all four date-range rows are off the ledger.
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_include_package_daily_breakdown_boundary__boundary_point[e2e_rest-string 'true' (non-boolean type)-"true"-invalid]
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_principal_ownership_partition__partition[e2e_rest-owner_mismatch-invalid]
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_reporting_dimensions_boundary__boundary_point[e2e_rest-geo with geo_level=metro but no system (behavioral gap)-{"geo": {"geo_level": "metro"}}-invalid]

# --- uc004 delivery account valid rows — GRADUATED (#1417 merge) ---
# Was: valid account rows referenced acc_acme_001 / acme-corp.com and no uc004
# step seeded them -> honest ACCOUNT_NOT_FOUND. #1417's seeded-account Given
# now provisions the referenced accounts and the harness
# realizes the seeding against the server DB; both rows XPASSed in-network
# (innet_140726_1516) and are retired with the boundary siblings.

# --- uc006 account billing-state resolution — GRADUATED (#1417 merge) ---
# Was: ACCOUNT_PAYMENT_REQUIRED / ACCOUNT_SETUP_REQUIRED / ACCOUNT_SUSPENDED /
# ACCOUNT_NOT_FOUND / ACCOUNT_AMBIGUOUS never raised by the live server (billing
# state not server-seeded). PR #1417's account-resolution + canonical error-code
# wiring makes all 12 pass in-network — xpass confirmed innet_040726_0013.

# --- uc011 account sync DB read-back — GRADUATED (#1430 items 1-2) ---
# Was: read-back assertions found 0 accounts in the server DB. Root cause was the
# wrong-DB class (integration_db repointed production's cached engine at an empty
# per-test DB while env factories wrote to the live server DB) plus tokenless agent
# identities being 401'd with the Given errors swallowed. Fixed by _db_scope_for
# (production engine pointed at the server DB over e2e) and the uc011 agent
# auth_token + fail-loudly Given fix; all 6 xpassed in-network (innet_050726_2030).

# --- get_products inventory-profile tenant duplicate — GRADUATED (#1430 jdy1-M3) ---
# Fixed by idempotent get-or-create given_tenant (uc_get_products_inventory.py); the 6
# entries now pass in-network (xpass confirmed innet_120626_1822).

# --- uc004 webhook retry/backoff/sequence observability — GRADUATED (#1430 jdy1-M4) ---
# retry-on-5xx/network, no-retry-on-4xx, sequence-monotonic assert on env.mock["post"]
# counts not visible over HTTP. Now declared impl-only by tag in
# _UC004_E2E_WEBHOOK_INTERNAL_TAGS (conftest) -> env-owned strict=False xfail, off the
# nodeid ledger (same mechanism as the other webhook-internal e2e scenarios).

# --- uc004 rows unmasked by the build_rest_body fix (9) ---
# Added innet_280726_1823 (build_rest_body fix, #1600). These are NOT new regressions: every one of them was passing only
# because the harness dropped the field before it reached the server, so the REST leg asserted
# against a response to a payload it never sent. Two distinct causes, both already ticketed:
#
# sampling_method (6) + ownership (1) — #1756. The steps dispatch request fields named
#   `sampling_method` and `ownership`. NEITHER EXISTS: not in GetMediaBuyDeliveryRequest, not in
#   GetMediaBuyDeliveryBody, and nowhere in the pinned adcp v3.1.1 tree
#   (`git grep sampling_method v3.1.1` returns nothing). The server now correctly answers
#   INVALID_REQUEST "Extra inputs are not permitted". impl/a2a/mcp have failed these all along and
#   are already ledgered in conftest; these are the e2e_rest twins. Retiring them means reconciling
#   the generated scenario upstream, NOT implementing a non-spec field.
#
# daily_breakdown (2) — #1711. media_buy_delivery.py:549 sets daily_breakdown=None
#   ("not calculated in this implementation"), so include_package_daily_breakdown=true returns 200
#   with no breakdown. The oracle previously read `daily`/`by_day` — field names that do not exist —
#   behind a presence guard, so it could not fail; #1751 made it derive its expectation
#   from the dispatched request. Retiring these means implementing the breakdown.
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_sampling_method_partition__partition[e2e_rest-random-random-valid]
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_sampling_method_partition__partition[e2e_rest-stratified-stratified-valid]
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_sampling_method_partition__partition[e2e_rest-recent-recent-valid]
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_sampling_method_partition__partition[e2e_rest-failures_only-failures_only-valid]
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_sampling_method_boundary__boundary_point[e2e_rest-random (first enum value)-random-valid]
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_sampling_method_boundary__boundary_point[e2e_rest-failures_only (last enum value)-failures_only-valid]
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_principal_ownership_boundary__boundary_point[e2e_rest-principal matches owner-valid]
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_include_package_daily_breakdown_partition__partition[e2e_rest-explicit_true-true-valid]
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_include_package_daily_breakdown_boundary__boundary_point[e2e_rest-true (explicit)-true-valid]

# --- explicit inline-xfail production gaps (2) ---
# GENUINE GAP (already inline-xfailed in the step body): seller attribution-default not
# implemented (production echoes the buyer request), and push_notification_config webhook
# registration acknowledgement not implemented. src/ feature work.
tests/bdd/test_uc004_deliver_media_buy_metrics.py::test_seller_ignores_attribution_request__returns_platform_default[e2e_rest]
tests/bdd/test_uc011_manage_accounts.py::test_push_notification_for_async_status_changes__with_push_notification[e2e_rest]

# --- uc004 roas/cpa aggregated_totals — RETIRED from the ledger (#1430 item 4),
# --- then GRADUATED outright ---
# The three Then steps are defined (uc004_delivery.py) and production now computes
# conversions/conversion_value/roas/cost_per_acquisition in aggregated_totals
# (media_buy_delivery.py DeliveryTotals aggregation, omit-on-zero quotients).
# T-UC-004-aggregated-roas-and-cpa is marked Graduated in conftest (no longer in
# _UC004_XFAIL_ADDITIONAL); the innet full-suite gate runs it green on all transports.

# --- uc002 creative extension scenarios, newly wired by #1417 — GRADUATED ---
# Imported from PR #1417's ledger at the media-buy-validation merge. ext-o/ext-p
# GRADUATED (#1430 item 3): over e2e the live tenant defaulted to the
# PENDING-approval path (different validation semantics than the auto path the
# scenarios assert); seeding auto-approval in the ext-o/ext-p Givens
# (_seed_auto_approval) makes both xpass in-network (innet_050726_2030).
# ext-q GRADUATED (#1430, upload mock-fidelity): the Given now seeds a REAL
# reference-catalog format (display_300x250_image, asset_id banner_image),
# MockAdServer.add_creative_assets honors a DB-injected fail_on_upload flag
# (shared _raise_injected_failure with fail_on_create/fail_on_update), and
# _get_format_spec_sync uses run_async_in_sync_context so server-side format
# resolution works inside the async transports at all (it previously ALWAYS
# failed with 'asyncio.run() cannot be called from a running event loop').
# Verified in-network: all 4 transports pass incl. e2e_rest.

# --- parallel e2e_rest rows (10) — GRADUATED (#1585 A4) ---
# Added 2026-07-09 when perf/parallelize-test-suite enabled parallel e2e_rest
# (E2E_PER_WORKER), annotated "mock-injection artifacts" (#1739). That annotation
# named THREE causes (mock-injection invisible, auth/tenant-context, wire not
# stashed) and all three had fixes land AFTER the failures were observed:
# set_adapter_response gained the _persist_simulation_config e2e realization the
# live Mock adapter reads back (src/adapters/mock_ad_server.py _load_delivery_
# simulation, under ADCP_TESTING); RestE2EDispatcher started stashing the real
# HTTP body as wire_response (9ac08dad3); and the uc018 Givens became per-scenario
# tenant + get_or_create + re-authenticated (6b1334ac7, d1e69d717 / #1430).
# Ledger xfails are xfail(strict=False) — an xpass is SILENT — so these had been
# passing unnoticed. Evidence: bdd-in-network run innet_290726_1225 graded all 24
# ledger rows: exactly these 10 xpassed, the other 14 still xfailed, 0 uncollected.
# Confirmed on the tree that CONTAINS this graduation by run innet_290726_1821
# (full suite exit=0): all 10 now report plain PASS — not xpass — and all 14
# remaining ledger rows still xfail, 0 uncollected, 0 failures in bdd_e2e.
#
# Inspected one scenario at a time per .claude/rules/workflows/xpass-graduation.md
# (an xpass can also mean the scenario is too weak to fail). Per row:
#  uc004 dim rows (4) — T-UC-004-dim-supported/-truncated/-complete/-multi. Given
#    goes through the shared harness (env.set_adapter_response), never a transport
#    branch. dim-supported was STRENGTHENED FIRST in this change: its Then step held
#    an imperative pytest.xfail() where the segmentation claim belongs, and armed
#    the distinctness check only for >1 entries — so one unsegmented aggregate row
#    reported xfail, never fail. Now a hard assert, distinctness armed at any
#    length, and the expected rows (mobile/desktop/tablet) pinned IN the scenario;
#    mutation-verified (collapsing the mock to one row fails the scenario). The
#    truncation row gained a non-empty floor so "at most 5" cannot pass on [].
#  uc005 format_id rows (3) — baseline shape / roundtrip-from-products /
#    third-party agent_url. Baseline and roundtrip assert on the real HTTP body
#    (wire_response) with a >=1 non-empty floor and verbatim (agent_url, id)
#    equality. Third-party asserts the seller does not substitute its own same-id
#    format; the falsifier is live over e2e because the reference catalog really
#    does serve display_300x250_image under the seller agent_url — an id-only
#    filter would return it and fail the step. Third-party was STRENGTHENED FIRST
#    in this change: its expected outcome was entirely negative, so it passed on an
#    empty formats[] for ANY reason (creative agent down, catalog drift, a 200
#    carrying errors). The scenario now carries a POSITIVE CONTROL Given — the same
#    call on the same transport with the SELLER's (agent_url, id) must resolve to
#    exactly that pair — and reads the wire like its two siblings. Mutation-verified
#    (production returning [] whenever format_ids is set fails it on a2a/mcp/rest;
#    before the control, that mutation passed).
#  uc018 rows (3) — BR-RULE-034 INV-1 + counter + concept_ids filter. All read the
#    wire creatives array (loud guard if a wire transport failed to stash), assert
#    exact set equality against self-seeded ids under a fresh per-scenario tenant,
#    and carry explicit anti-vacuity guards on the empty response.
# Route 3 (E2EUnsupportedSetup) was considered and REJECTED: it asserts a setup
# METHOD has no live-server surface, which these xpasses prove false, and it would
# mis-declare set_adapter_response / set_registry_formats for every other scenario
# that seeds through them. 3 of the 10 have no setup method to declare on at all.
