diff --git a/node_modules/@adcp/sdk/dist/lib/protocols/a2a.js b/node_modules/@adcp/sdk/dist/lib/protocols/a2a.js
index 1c7fba9..a8bd04c 100644
--- a/node_modules/@adcp/sdk/dist/lib/protocols/a2a.js
+++ b/node_modules/@adcp/sdk/dist/lib/protocols/a2a.js
@@ -206,7 +206,7 @@ async function createA2AClientFromCardUrl(cardUrl, fetchImpl) {
   return createNativeA2AClientFromCardUrl(cardUrl, fetchImpl);
 }
 async function createNativeA2AClientFromCardUrl(cardUrl, fetchImpl) {
-  const legacyCompat = { enabled: true };
+  const legacyCompat = { enabled: false };
   const factory = new import_client.ClientFactory({
     transports: [new import_client.JsonRpcTransportFactory({ fetchImpl, legacyCompat })],
     cardResolver: new import_client.DefaultAgentCardResolver({ fetchImpl, legacyCompat })
diff --git a/node_modules/@adcp/sdk/dist/lib/protocols/a2a.mjs b/node_modules/@adcp/sdk/dist/lib/protocols/a2a.mjs
index 0840718..e6714a3 100644
--- a/node_modules/@adcp/sdk/dist/lib/protocols/a2a.mjs
+++ b/node_modules/@adcp/sdk/dist/lib/protocols/a2a.mjs
@@ -190,7 +190,7 @@ async function createA2AClientFromCardUrl(cardUrl, fetchImpl) {
   return createNativeA2AClientFromCardUrl(cardUrl, fetchImpl);
 }
 async function createNativeA2AClientFromCardUrl(cardUrl, fetchImpl) {
-  const legacyCompat = { enabled: true };
+  const legacyCompat = { enabled: false };
   const factory = new ClientFactory({
     transports: [new JsonRpcTransportFactory({ fetchImpl, legacyCompat })],
     cardResolver: new DefaultAgentCardResolver({ fetchImpl, legacyCompat })
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/probes.js b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/probes.js
index 1e1024c..a4b0699 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/probes.js
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/probes.js
@@ -337,7 +337,7 @@ async function rawMcpProbe(options) {
   return { httpResult, taskResult: taskResultFromRpc(httpResult, parsed) };
 }
 async function rawA2aProbe(options) {
-  const { agentUrl, method, params, headers = {}, allowPrivateIp = false } = options;
+  const { agentUrl, method, params, headers = {}, allowPrivateIp = false, fetchFn } = options;
   const body = JSON.stringify({
     jsonrpc: "2.0",
     id: ++probeRequestId,
@@ -354,7 +354,8 @@ async function rawA2aProbe(options) {
         ...headers
       },
       body,
-      allowPrivateIp
+      allowPrivateIp,
+      ...fetchFn ? { trustedFetchFn: fetchFn } : {}
     });
     httpResult.status = res.status;
     httpResult.headers = res.headers;
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/probes.mjs b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/probes.mjs
index 156d094..5d15e2b 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/probes.mjs
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/probes.mjs
@@ -310,7 +310,7 @@ async function rawMcpProbe(options) {
   return { httpResult, taskResult: taskResultFromRpc(httpResult, parsed) };
 }
 async function rawA2aProbe(options) {
-  const { agentUrl, method, params, headers = {}, allowPrivateIp = false } = options;
+  const { agentUrl, method, params, headers = {}, allowPrivateIp = false, fetchFn } = options;
   const body = JSON.stringify({
     jsonrpc: "2.0",
     id: ++probeRequestId,
@@ -327,7 +327,8 @@ async function rawA2aProbe(options) {
         ...headers
       },
       body,
-      allowPrivateIp
+      allowPrivateIp,
+      ...fetchFn ? { trustedFetchFn: fetchFn } : {}
     });
     httpResult.status = res.status;
     httpResult.headers = res.headers;
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/builder.js b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/builder.js
index 25bc151..49e2d8a 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/builder.js
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/builder.js
@@ -257,8 +257,54 @@ function retargetUrl(vectorUrl, baseUrl) {
   }
   return v.toString();
 }
+function a2aEndpoint(baseUrl) {
+  const u = new URL(baseUrl);
+  // A2A is CARD-FIRST, so the RPC endpoint is normally read off
+  // /.well-known/agent-card.json. The signing probe is bearer-less and deliberately does
+  // not fetch the card, so it appends the conventional mount when the base carries no
+  // path. KNOWN CEILING: an agent serving A2A somewhere other than /a2a must pass the
+  // endpoint as the storyboard's a2a agent url.
+  if (u.pathname === "" || u.pathname === "/") u.pathname = "/a2a";
+  return u.toString();
+}
+function wrapA2aEnvelope(operation, rawBody, idOverride) {
+  // The NATIVE 1.0 SendMessage frame, not the 0.3 `message/send` family: an agent that
+  // builds its JSON-RPC routes without the 0.3 compat adapter does not serve that family
+  // at all. Field names are proto JSON (`messageId`, `ROLE_USER`), because the dispatcher
+  // ParseDicts `params` back into its generated model.
+  const args = rawBody && rawBody.length > 0 ? JSON.parse(rawBody) : {};
+  return JSON.stringify({
+    jsonrpc: "2.0",
+    id: idOverride ?? (0, import_crypto.randomUUID)(),
+    method: "SendMessage",
+    params: {
+      message: {
+        messageId: (0, import_crypto.randomUUID)(),
+        role: "ROLE_USER",
+        parts: [{ data: { skill: operation, parameters: args } }]
+      }
+    }
+  });
+}
 function applyTransport(vector, options) {
   const headers = { ...vector.request.headers };
+  if (options.transport === "a2a") {
+    if (!options.baseUrl) {
+      throw new Error(`transport: 'a2a' requires a baseUrl (the agent's base url, e.g. https://agent.example)`);
+    }
+    const operation = extractOperationFromVectorUrl(vector.request.url);
+    // MERGED BEFORE THE SIGNATURE IS COMPUTED, and that ordering is the whole point: the
+    // signature base covers the headers and Content-Digest covers the body, so a version
+    // header added after signing would be outside the base on a request the verifier
+    // reconstructs WITH it.
+    headers["A2A-Version"] = "1.0";
+    return {
+      method: vector.request.method,
+      url: a2aEndpoint(options.baseUrl),
+      headers,
+      body: wrapA2aEnvelope(operation, vector.request.body, options.mcpJsonRpcId)
+    };
+  }
   if (options.transport === "mcp") {
     if (!options.baseUrl) {
       throw new Error(`transport: 'mcp' requires a baseUrl (the MCP endpoint, e.g. http://agent/mcp)`);
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/builder.mjs b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/builder.mjs
index 5a98e85..c46b634 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/builder.mjs
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/builder.mjs
@@ -239,8 +239,54 @@ function retargetUrl(vectorUrl, baseUrl) {
   }
   return v.toString();
 }
+function a2aEndpoint(baseUrl) {
+  const u = new URL(baseUrl);
+  // A2A is CARD-FIRST, so the RPC endpoint is normally read off
+  // /.well-known/agent-card.json. The signing probe is bearer-less and deliberately does
+  // not fetch the card, so it appends the conventional mount when the base carries no
+  // path. KNOWN CEILING: an agent serving A2A somewhere other than /a2a must pass the
+  // endpoint as the storyboard's a2a agent url.
+  if (u.pathname === "" || u.pathname === "/") u.pathname = "/a2a";
+  return u.toString();
+}
+function wrapA2aEnvelope(operation, rawBody, idOverride) {
+  // The NATIVE 1.0 SendMessage frame, not the 0.3 `message/send` family: an agent that
+  // builds its JSON-RPC routes without the 0.3 compat adapter does not serve that family
+  // at all. Field names are proto JSON (`messageId`, `ROLE_USER`), because the dispatcher
+  // ParseDicts `params` back into its generated model.
+  const args = rawBody && rawBody.length > 0 ? JSON.parse(rawBody) : {};
+  return JSON.stringify({
+    jsonrpc: "2.0",
+    id: idOverride ?? randomUUID(),
+    method: "SendMessage",
+    params: {
+      message: {
+        messageId: randomUUID(),
+        role: "ROLE_USER",
+        parts: [{ data: { skill: operation, parameters: args } }]
+      }
+    }
+  });
+}
 function applyTransport(vector, options) {
   const headers = { ...vector.request.headers };
+  if (options.transport === "a2a") {
+    if (!options.baseUrl) {
+      throw new Error(`transport: 'a2a' requires a baseUrl (the agent's base url, e.g. https://agent.example)`);
+    }
+    const operation = extractOperationFromVectorUrl(vector.request.url);
+    // MERGED BEFORE THE SIGNATURE IS COMPUTED, and that ordering is the whole point: the
+    // signature base covers the headers and Content-Digest covers the body, so a version
+    // header added after signing would be outside the base on a request the verifier
+    // reconstructs WITH it.
+    headers["A2A-Version"] = "1.0";
+    return {
+      method: vector.request.method,
+      url: a2aEndpoint(options.baseUrl),
+      headers,
+      body: wrapA2aEnvelope(operation, vector.request.body, options.mcpJsonRpcId)
+    };
+  }
   if (options.transport === "mcp") {
     if (!options.baseUrl) {
       throw new Error(`transport: 'mcp' requires a baseUrl (the MCP endpoint, e.g. http://agent/mcp)`);
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/grader.js b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/grader.js
index c23ec39..5f628fa 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/grader.js
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/grader.js
@@ -162,7 +162,11 @@ function preflightSkip(vector, kind, contract, options) {
   if (transportReason) {
     return { ...base, skipped: true, skip_reason: "transport_ungradable", diagnostic: transportReason };
   }
-  if (kind === "positive" && (options.transport ?? "mcp") === "mcp" && MCP_FLATTENED_VECTORS.has(vector.id)) {
+  // BOTH ENVELOPED TRANSPORTS flatten these edges, not just mcp: a2a routes every vector to
+  // the one JSON-RPC endpoint exactly as mcp does, so the port/path/query/encoding the
+  // vector exists to grade never reaches the wire either way.
+  const envelopedTransport = (options.transport ?? "mcp") === "mcp" || (options.transport ?? "mcp") === "a2a";
+  if (kind === "positive" && envelopedTransport && MCP_FLATTENED_VECTORS.has(vector.id)) {
     return {
       ...base,
       skipped: true,
@@ -460,12 +464,35 @@ function contentDigestPolicyMismatch(vector, kind, agentPolicy) {
   }
   return contentDigestStructuralMismatch(vector, agentPolicy);
 }
+// The two refusals that EXIST ONLY under a narrowed content-digest policy: one for a
+// signature that omits the digest (required), one for a signature that covers it
+// (forbidden). Everything else a digest vector can assert -- a 2xx, a digest MISMATCH, a
+// malformed digest header -- is a property of the verifier, not of the policy.
+const CONTENT_DIGEST_POLICY_REFUSALS = /* @__PURE__ */ new Set([
+  "request_signature_components_incomplete",
+  "request_signature_components_unexpected"
+]);
+function gradableUnderEither(vector, agentCap) {
+  // `either` is the WIDEST policy, not a third incompatible one: security.mdx defines it as
+  // "signer chooses; verifier accepts both covered and uncovered forms". An agent declaring
+  // it therefore grades every digest vector EXCEPT the two whose expected outcome is the
+  // narrowing itself -- there is no covers_content_digest value under which an `either`
+  // agent refuses a request for covering, or for not covering.
+  //
+  // Without this, comparing the vector's declared policy to the agent's excluded any vector
+  // that so much as MENTIONS content-digest, including `positive/002` (a covered digest that
+  // must be ACCEPTED) and `negative/010` (a FALSIFIED digest that must be rejected). Both
+  // were passing before the capability filter was wired and both stopped being graded by it
+  // -- a clean number bought by dropping the vectors that test the thing.
+  if (agentCap.covers_content_digest !== "either") return false;
+  return !CONTENT_DIGEST_POLICY_REFUSALS.has(vector.expected_error_code);
+}
 function capabilityMismatch(vector, agentCap) {
   const vectorCap = vector.verifier_capability;
   if (vectorCap.supported !== agentCap.supported) {
     return `Vector asserts supported=${vectorCap.supported} but agent declares supported=${agentCap.supported}. Verify the agent's request_signing capability block.`;
   }
-  if (vectorCap.covers_content_digest !== "either" && vectorCap.covers_content_digest !== agentCap.covers_content_digest) {
+  if (vectorCap.covers_content_digest !== "either" && vectorCap.covers_content_digest !== agentCap.covers_content_digest && !gradableUnderEither(vector, agentCap)) {
     return `Vector asserts covers_content_digest='${vectorCap.covers_content_digest}' but agent declares '${agentCap.covers_content_digest}'. The vector can't grade against this profile \u2014 its expected verifier behavior doesn't match what the agent implements.`;
   }
   const structural = contentDigestStructuralMismatch(vector, agentCap.covers_content_digest);
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/grader.mjs b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/grader.mjs
index 0563d6d..2da38ec 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/grader.mjs
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/grader.mjs
@@ -144,7 +144,11 @@ function preflightSkip(vector, kind, contract, options) {
   if (transportReason) {
     return { ...base, skipped: true, skip_reason: "transport_ungradable", diagnostic: transportReason };
   }
-  if (kind === "positive" && (options.transport ?? "mcp") === "mcp" && MCP_FLATTENED_VECTORS.has(vector.id)) {
+  // BOTH ENVELOPED TRANSPORTS flatten these edges, not just mcp: a2a routes every vector to
+  // the one JSON-RPC endpoint exactly as mcp does, so the port/path/query/encoding the
+  // vector exists to grade never reaches the wire either way.
+  const envelopedTransport = (options.transport ?? "mcp") === "mcp" || (options.transport ?? "mcp") === "a2a";
+  if (kind === "positive" && envelopedTransport && MCP_FLATTENED_VECTORS.has(vector.id)) {
     return {
       ...base,
       skipped: true,
@@ -442,12 +446,35 @@ function contentDigestPolicyMismatch(vector, kind, agentPolicy) {
   }
   return contentDigestStructuralMismatch(vector, agentPolicy);
 }
+// The two refusals that EXIST ONLY under a narrowed content-digest policy: one for a
+// signature that omits the digest (required), one for a signature that covers it
+// (forbidden). Everything else a digest vector can assert -- a 2xx, a digest MISMATCH, a
+// malformed digest header -- is a property of the verifier, not of the policy.
+const CONTENT_DIGEST_POLICY_REFUSALS = /* @__PURE__ */ new Set([
+  "request_signature_components_incomplete",
+  "request_signature_components_unexpected"
+]);
+function gradableUnderEither(vector, agentCap) {
+  // `either` is the WIDEST policy, not a third incompatible one: security.mdx defines it as
+  // "signer chooses; verifier accepts both covered and uncovered forms". An agent declaring
+  // it therefore grades every digest vector EXCEPT the two whose expected outcome is the
+  // narrowing itself -- there is no covers_content_digest value under which an `either`
+  // agent refuses a request for covering, or for not covering.
+  //
+  // Without this, comparing the vector's declared policy to the agent's excluded any vector
+  // that so much as MENTIONS content-digest, including `positive/002` (a covered digest that
+  // must be ACCEPTED) and `negative/010` (a FALSIFIED digest that must be rejected). Both
+  // were passing before the capability filter was wired and both stopped being graded by it
+  // -- a clean number bought by dropping the vectors that test the thing.
+  if (agentCap.covers_content_digest !== "either") return false;
+  return !CONTENT_DIGEST_POLICY_REFUSALS.has(vector.expected_error_code);
+}
 function capabilityMismatch(vector, agentCap) {
   const vectorCap = vector.verifier_capability;
   if (vectorCap.supported !== agentCap.supported) {
     return `Vector asserts supported=${vectorCap.supported} but agent declares supported=${agentCap.supported}. Verify the agent's request_signing capability block.`;
   }
-  if (vectorCap.covers_content_digest !== "either" && vectorCap.covers_content_digest !== agentCap.covers_content_digest) {
+  if (vectorCap.covers_content_digest !== "either" && vectorCap.covers_content_digest !== agentCap.covers_content_digest && !gradableUnderEither(vector, agentCap)) {
     return `Vector asserts covers_content_digest='${vectorCap.covers_content_digest}' but agent declares '${agentCap.covers_content_digest}'. The vector can't grade against this profile \u2014 its expected verifier behavior doesn't match what the agent implements.`;
   }
   const structural = contentDigestStructuralMismatch(vector, agentCap.covers_content_digest);
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/probe-dispatch.js b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/probe-dispatch.js
index 5357dd3..91b03c1 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/probe-dispatch.js
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/probe-dispatch.js
@@ -25,8 +25,13 @@ module.exports = __toCommonJS(probe_dispatch_exports);
 var import_grader = require('./grader.js');
 var import_synthesize = require('./synthesize.js');
 var import_vector_loader = require('./vector-loader.js');
-function resolveVectorTransport(rsOpts) {
-  return rsOpts.transport ?? "mcp";
+function resolveVectorTransport(rsOpts, protocol) {
+  // The RUN's protocol, not a hard "mcp". Defaulting to mcp on the a2a card fires an MCP
+  // initialize at the A2A url, which an agent answers with its framework's 405 page, and
+  // every vector throws in the precondition before a single one is graded -- an entire
+  // surface reported as broken when nothing about it was exercised.
+  // `raw` stays the explicit opt-out for a per-operation HTTP surface.
+  return rsOpts.transport ?? (protocol === "a2a" ? "a2a" : "mcp");
 }
 async function probeRequestSigningVector(stepId, agentUrl, options) {
   const parsed = (0, import_synthesize.parseRequestSigningStepId)(stepId);
@@ -66,11 +71,18 @@ async function probeRequestSigningVector(stepId, agentUrl, options) {
       onlyVectors: rsOpts.onlyVectors,
       skipVectors: rsOpts.skipVectors,
       skipRateAbuse: rsOpts.skipRateAbuse,
-      transport: resolveVectorTransport(rsOpts),
+      transport: resolveVectorTransport(rsOpts, options.protocol),
       // The auto-initialize handshake authenticates like any MCP client;
       // agents commonly require auth on `initialize` (the signed vectors
       // themselves stay bearer-less — the signature is their auth).
       ...options.auth?.type === "bearer" && options.auth.token ? { initializeHeaders: { authorization: `Bearer ${options.auth.token}` } } : {},
+      // The agent's DECLARED request_signing block, so `capabilityMismatch` can do the job
+      // it already exists to do. Without it every vector is graded against the agent
+      // regardless of whether the agent implements the profile the vector asserts, and a
+      // vector written for a different profile FAILS instead of skipping -- which is not a
+      // verdict about the agent at all. `_profile.raw_capabilities` is the same object the
+      // `fetch_brand_jwks` task reads two branches away in the runner's step dispatcher.
+      agentCapability: options._profile?.raw_capabilities?.request_signing,
       mcpSessionId: rsOpts.mcpSessionId,
       mcpProtocolVersion: rsOpts.mcpProtocolVersion
     });
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/probe-dispatch.mjs b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/probe-dispatch.mjs
index e40744a..40e2106 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/probe-dispatch.mjs
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/probe-dispatch.mjs
@@ -1,8 +1,13 @@
 import { gradeOneVector } from "./grader.mjs";
 import { parseRequestSigningStepId } from "./synthesize.mjs";
 import { loadRequestSigningVectors } from "./vector-loader.mjs";
-function resolveVectorTransport(rsOpts) {
-  return rsOpts.transport ?? "mcp";
+function resolveVectorTransport(rsOpts, protocol) {
+  // The RUN's protocol, not a hard "mcp". Defaulting to mcp on the a2a card fires an MCP
+  // initialize at the A2A url, which an agent answers with its framework's 405 page, and
+  // every vector throws in the precondition before a single one is graded -- an entire
+  // surface reported as broken when nothing about it was exercised.
+  // `raw` stays the explicit opt-out for a per-operation HTTP surface.
+  return rsOpts.transport ?? (protocol === "a2a" ? "a2a" : "mcp");
 }
 async function probeRequestSigningVector(stepId, agentUrl, options) {
   const parsed = parseRequestSigningStepId(stepId);
@@ -42,11 +47,18 @@ async function probeRequestSigningVector(stepId, agentUrl, options) {
       onlyVectors: rsOpts.onlyVectors,
       skipVectors: rsOpts.skipVectors,
       skipRateAbuse: rsOpts.skipRateAbuse,
-      transport: resolveVectorTransport(rsOpts),
+      transport: resolveVectorTransport(rsOpts, options.protocol),
       // The auto-initialize handshake authenticates like any MCP client;
       // agents commonly require auth on `initialize` (the signed vectors
       // themselves stay bearer-less — the signature is their auth).
       ...options.auth?.type === "bearer" && options.auth.token ? { initializeHeaders: { authorization: `Bearer ${options.auth.token}` } } : {},
+      // The agent's DECLARED request_signing block, so `capabilityMismatch` can do the job
+      // it already exists to do. Without it every vector is graded against the agent
+      // regardless of whether the agent implements the profile the vector asserts, and a
+      // vector written for a different profile FAILS instead of skipping -- which is not a
+      // verdict about the agent at all. `_profile.raw_capabilities` is the same object the
+      // `fetch_brand_jwks` task reads two branches away in the runner's step dispatcher.
+      agentCapability: options._profile?.raw_capabilities?.request_signing,
       mcpSessionId: rsOpts.mcpSessionId,
       mcpProtocolVersion: rsOpts.mcpProtocolVersion
     });
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/synthesize.js b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/synthesize.js
index 48ea68a..6369ad6 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/synthesize.js
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/synthesize.js
@@ -65,7 +65,12 @@ function synthesizeNegativeStep(vector) {
     title: `Negative: ${vector.name}`,
     task: REQUEST_SIGNING_PROBE_TASK,
     narrative: `Build vector ${vector.id} with its documented mutation and send to the agent; expect 401 with WWW-Authenticate: Signature error="${vector.expected_error_code}".`,
-    validations: [
+    // A vector carrying `jwks_override` is graded IN-LIBRARY by `gradeJwksOverrideNegative`
+    // -- it presents its own JWKS to the local verifier and never reaches the agent, so it
+    // reports `http_status: 0`. Synthesizing a 401 status validation for it asserts
+    // something that grading path cannot produce: the vector passes the grader and fails the
+    // step. Its verdict still lands, on the probe's `error` field like every other vector's.
+    validations: vector.jwks_override ? [] : [
       {
         check: "http_status",
         value: 401,
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/synthesize.mjs b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/synthesize.mjs
index 1e13c0a..c9f9330 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/synthesize.mjs
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/request-signing/synthesize.mjs
@@ -38,7 +38,12 @@ function synthesizeNegativeStep(vector) {
     title: `Negative: ${vector.name}`,
     task: REQUEST_SIGNING_PROBE_TASK,
     narrative: `Build vector ${vector.id} with its documented mutation and send to the agent; expect 401 with WWW-Authenticate: Signature error="${vector.expected_error_code}".`,
-    validations: [
+    // A vector carrying `jwks_override` is graded IN-LIBRARY by `gradeJwksOverrideNegative`
+    // -- it presents its own JWKS to the local verifier and never reaches the agent, so it
+    // reports `http_status: 0`. Synthesizing a 401 status validation for it asserts
+    // something that grading path cannot produce: the vector passes the grader and fails the
+    // step. Its verdict still lands, on the probe's `error` field like every other vector's.
+    validations: vector.jwks_override ? [] : [
       {
         check: "http_status",
         value: 401,
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/runner.js b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/runner.js
index e9b14ed..91f477c 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/runner.js
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/runner.js
@@ -3086,7 +3086,34 @@ async function executeStep(client, step, storyboardId, phaseId, context, allStep
   if (useRawProbe) {
     const started = Date.now();
     try {
-      const probe = await (0, import_probes.rawMcpProbe)({
+      // A raw auth probe speaks the protocol under test. On A2A that is a 1.0 SendMessage
+      // JSON-RPC call at the RPC endpoint the agent card names; an MCP tools/call posted at
+      // the A2A agent URL is answered -32601 with HTTP 200 and grades nothing.
+      const rawA2aAuthProbe = async () => {
+        const card = await (0, import_probes.fetchProbe)(new URL("/.well-known/agent-card.json", runState.agentUrl).toString(), {
+          allowPrivateIp: options.allow_http === true,
+          fetchFn: options.transport?.trustedFetchFn
+        });
+        const rpcUrl = card.body?.supportedInterfaces?.[0]?.url;
+        if (typeof rpcUrl !== "string") {
+          return { httpResult: { ...card, error: card.error ?? "agent card names no supportedInterfaces[0].url" } };
+        }
+        return (0, import_probes.rawA2aProbe)({
+          agentUrl: rpcUrl,
+          method: "SendMessage",
+          params: {
+            message: {
+              messageId: `probe-${Date.now()}`,
+              role: "ROLE_USER",
+              parts: [{ data: { skill: effectiveStep.task, input: request ?? {} } }]
+            }
+          },
+          headers: { "a2a-version": "1.0", ...rawProbeHeaders },
+          allowPrivateIp: options.allow_http === true,
+          fetchFn: options.transport?.trustedFetchFn
+        });
+      };
+      const probe = options.protocol === "a2a" ? await rawA2aAuthProbe() : await (0, import_probes.rawMcpProbe)({
         agentUrl: runState.agentUrl,
         toolName: effectiveStep.task,
         args: request,
@@ -3104,7 +3131,7 @@ async function executeStep(client, step, storyboardId, phaseId, context, allStep
       };
       const filteredHeaders = filterResponseHeaders(httpResult.headers);
       responseRecord = {
-        transport: "mcp",
+        transport: options.protocol === "a2a" ? "a2a" : "mcp",
         payload: (0, import_redact_secrets.redactSecrets)(httpResult.body),
         ...typeof httpResult.status === "number" ? { status: httpResult.status } : {},
         ...filteredHeaders && { headers: filteredHeaders },
@@ -3197,7 +3224,7 @@ async function executeStep(client, step, storyboardId, phaseId, context, allStep
     }
   }
   const requestRecord = {
-    transport: useRawProbe ? "mcp" : options.protocol === "a2a" ? "a2a" : "mcp",
+    transport: options.protocol === "a2a" ? "a2a" : "mcp",
     operation: effectiveStep.task,
     payload: (0, import_redact_secrets.redactSecrets)(request),
     ...runState.agentUrl ? { url: (0, import_oauth_metadata_graph.redactOAuthUrlForOutput)(runState.agentUrl) } : {}
diff --git a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/runner.mjs b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/runner.mjs
index 7b795b1..cb7d722 100644
--- a/node_modules/@adcp/sdk/dist/lib/testing/storyboard/runner.mjs
+++ b/node_modules/@adcp/sdk/dist/lib/testing/storyboard/runner.mjs
@@ -62,6 +62,7 @@ import {
   probeOauthAuthServerMetadata,
   fetchProbe,
   rawMcpProbe,
+  rawA2aProbe,
   generateRandomInvalidApiKey,
   generateRandomInvalidJwt
 } from "./probes.mjs";
@@ -3103,7 +3104,34 @@ async function executeStep(client, step, storyboardId, phaseId, context, allStep
   if (useRawProbe) {
     const started = Date.now();
     try {
-      const probe = await rawMcpProbe({
+      // A raw auth probe speaks the protocol under test. On A2A that is a 1.0 SendMessage
+      // JSON-RPC call at the RPC endpoint the agent card names; an MCP tools/call posted at
+      // the A2A agent URL is answered -32601 with HTTP 200 and grades nothing.
+      const rawA2aAuthProbe = async () => {
+        const card = await fetchProbe(new URL("/.well-known/agent-card.json", runState.agentUrl).toString(), {
+          allowPrivateIp: options.allow_http === true,
+          fetchFn: options.transport?.trustedFetchFn
+        });
+        const rpcUrl = card.body?.supportedInterfaces?.[0]?.url;
+        if (typeof rpcUrl !== "string") {
+          return { httpResult: { ...card, error: card.error ?? "agent card names no supportedInterfaces[0].url" } };
+        }
+        return rawA2aProbe({
+          agentUrl: rpcUrl,
+          method: "SendMessage",
+          params: {
+            message: {
+              messageId: `probe-${Date.now()}`,
+              role: "ROLE_USER",
+              parts: [{ data: { skill: effectiveStep.task, input: request ?? {} } }]
+            }
+          },
+          headers: { "a2a-version": "1.0", ...rawProbeHeaders },
+          allowPrivateIp: options.allow_http === true,
+          fetchFn: options.transport?.trustedFetchFn
+        });
+      };
+      const probe = options.protocol === "a2a" ? await rawA2aAuthProbe() : await rawMcpProbe({
         agentUrl: runState.agentUrl,
         toolName: effectiveStep.task,
         args: request,
@@ -3121,7 +3149,7 @@ async function executeStep(client, step, storyboardId, phaseId, context, allStep
       };
       const filteredHeaders = filterResponseHeaders(httpResult.headers);
       responseRecord = {
-        transport: "mcp",
+        transport: options.protocol === "a2a" ? "a2a" : "mcp",
         payload: redactSecrets(httpResult.body),
         ...typeof httpResult.status === "number" ? { status: httpResult.status } : {},
         ...filteredHeaders && { headers: filteredHeaders },
@@ -3214,7 +3242,7 @@ async function executeStep(client, step, storyboardId, phaseId, context, allStep
     }
   }
   const requestRecord = {
-    transport: useRawProbe ? "mcp" : options.protocol === "a2a" ? "a2a" : "mcp",
+    transport: options.protocol === "a2a" ? "a2a" : "mcp",
     operation: effectiveStep.task,
     payload: redactSecrets(request),
     ...runState.agentUrl ? { url: redactOAuthUrlForOutput(runState.agentUrl) } : {}
